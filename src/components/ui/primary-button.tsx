/**
 * Shared gradient CTA used for availability actions (dashboard, profile self-card, subcontractors).
 * Keep in sync with primary "List / Update availability" buttons.
 */
export const primaryButtonClass =
  'h-10 rounded-full gap-2 px-5 shadow-md hover:shadow-lg bg-gradient-to-r from-blue-600 to-indigo-600 text-white hover:from-blue-600 hover:to-indigo-700 transition-all';
